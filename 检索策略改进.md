我看完了你的检索链路。整体设计已经比较完整，尤其是 **FTS5 + jieba + Agent tool + 课程 scope** 这套架构，对于课程知识库 Agent 是合理的。但如果目标是“论文阅读/个人知识库 Agent”这种更强的场景，目前主要瓶颈不是 FTS，而是**召回质量、上下文组织和 Agent 检索策略**。

我按优先级给建议：

---

## P0：最值得改的几个点（收益最大）

## 1. 增加「混合检索」：FTS5 + Embedding（最高优先级）

你现在：

```
query
 ↓
jieba
 ↓
FTS5 BM25
 ↓
top 8
```

这是典型 keyword retrieval。

问题：

> 用户问“为什么 Rust 不允许悬垂引用？”
>
> 笔记写：
>
> “生命周期保证引用不会超过被引用对象的有效范围”

你的系统可能完全找不到。

因为：

```
悬垂引用 != 生命周期保证
```

这是知识库最常见的问题。

建议改：

```
                query
                  |
        ---------------------
        |                   |
     FTS5 BM25          embedding
        |                   |
      top 20              top 20
        |                   |
        ------ merge -------
               |
           reranker
               |
            top 8
```

比如：

```
score =
0.5 * bm25_score
+
0.5 * cosine_similarity
```

甚至不用 reranker，第一版就能提升巨大。

---

## 2. 不要只返回 note，增加 chunk 层

你现在粒度：

```
note
 |
 + 全文
```

问题：

一个 Rust 笔记：

```
# Ownership

第一页:
所有权规则

第二页:
move

第三页:
borrow checker

第四页:
生命周期
```

搜索：

```
borrow checker
```

命中这个 note。

然后返回：

```
前2000字
```

实际上给 LLM：

```
第一页 ownership 定义
```

完全错。

建议：

入库变成：

```
note
 |
 + chunk_1
 |
 + chunk_2
 |
 + chunk_3
```

例如：

每 300~800 token 一个 chunk。

数据库：

```
notes

chunks
------
id
note_id
content
embedding
fts_content
heading
position
```

搜索：

```
query
 ↓
chunk retrieval
 ↓
返回 chunk
 ↓
LLM
```

这是 RAG 最核心改进。

---

## 3. 去掉固定 2000 chars，改成 token budget

现在：

```
take 2000 chars
```

问题：

中文：

2000 chars ≈ 2000 tokens

英文：

2000 chars ≈ 500 tokens

不稳定。

建议：

改：

```
max_context_tokens = 3000
```

然后：

```
while tokens < budget:
    append chunk
```

例如：

搜索结果：

```
[1]
chunk A 600 tokens

[2]
chunk B 800 tokens

[3]
chunk C 700 tokens
```

总计：

2100 tokens

继续加。

---

# P1：检索质量提升

## 4. concepts 表应该参与召回

你已经有：

```
concepts
338 个
```

这是非常宝贵的信息。

现在：

```
concepts
      |
      X
      |
search
```

浪费了。

建议：

建立：

```
concepts_fts
```

或者：

查询扩展：

用户：

```
什么是所有权
```

先查：

concept:

```
所有权
```

得到：

```
aliases:
ownership
move
borrow
copy trait
```

扩展 query：

```
所有权 OR ownership OR move OR borrow
```

效果：

非常明显。

---

## 5. query rewrite

现在：

用户：

> 为什么 Rust 需要生命周期？

直接：

```
jieba.cut()
```

问题：

自然语言问题包含大量噪声。

增加一步：

LLM query rewrite：

例如：

用户：

```
为什么 Rust 需要生命周期？
```

rewrite:

```
["生命周期", "borrow checker", "reference validity"]
```

然后搜索。

注意：

不要让 LLM 直接回答，而是：

```
用户query
 ↓
small LLM
 ↓
search query
 ↓
retrieval
```

成本很低。

---

## 6. OR 降级逻辑可以优化

现在：

```
term1 count
term2 count
term3 count
...
```

N 次 SQL。

可以直接：

SQLite FTS5 支持：

```
fts5vocab
```

或者：

存：

```
term -> doc frequency
```

自己维护 inverted index。

不过这个属于性能优化。

你的规模：

338 concepts

课程笔记级别

目前不用急。

---

# P2：Agent 层改进

## 7. 不要限制 search_notes 一轮只能调用一次

这个设计：

> 防止模型反复搜索

思路对。

但是副作用很大。

真实 Agent：

第一轮：

```
search("ownership")
```

发现：

```
没有找到生命周期
```

应该允许：

第二轮：

```
search("borrow checker lifetime")
```

建议：

改成：

```
max_search_calls = 3

但是：
query 不允许重复
```

例如：

允许：

```
ownership
ownership move
ownership borrow
```

禁止：

```
ownership
ownership
ownership
```

---

## 8. 增加 search feedback

现在：

tool 返回：

```
results
```

建议增加：

```
metadata:
{
 query,
 hit_count,
 avg_score,
 covered_terms
}
```

如果：

```
hit_count=0
```

Agent 应该知道：

需要换关键词。

否则模型不知道：

“没搜到”和“搜到了但不相关”的区别。

---

# P3：引用系统

## 9. 引用不要绑定 note id，绑定 chunk id

现在：

```
[1]
```

对应：

```
note
```

未来：

最好：

```
[1]
Rust Ownership.md#chunk3
```

这样：

用户追溯：

```
点击引用
↓
跳到原文位置
```

知识库产品体验提升巨大。

---

# P4：你的架构里一个隐藏问题

## 10. notes.content 和 notes_fts.content 分离很好，但是清洗策略可能过度

你现在：

```
clean_for_fts

剥:
- wikilink
- 目录
- 元信息
```

这个方向正确。

但是：

目录其实可能非常有价值。

例如：

Markdown：

```
## 生命周期

## 为什么需要生命周期

## borrow checker
```

标题本身就是强语义。

建议：

FTS 内容：

不要：

```
正文
```

而是：

```
title
heading
tags
正文
```

权重：

```
title x3
heading x2
content x1
```

FTS5 可以做到。

---

# 我觉得你的下一步路线

如果我是你，我不会马上做 embedding。

按收益排序：

```
① chunk 化
★★★★★

② title/heading 加权 FTS
★★★★★

③ concepts query expansion
★★★★

④ search 多轮调用
★★★★

⑤ embedding hybrid retrieval
★★★★

⑥ reranker
★★★
```

原因：

你现在最大的问题不是“搜不到”，而是：

> 搜到了正确笔记，但是给 LLM 的上下文不是最相关部分。

chunk 化 + heading 权重会立刻改善。

---

整体评价：

你的 M7 版本已经超过很多课程项目的 RAG baseline 了。现在更像是在从：

```
关键词搜索系统
```

升级到：

```
真正的知识库 Agent
```

关键转折点就是 **chunk + hybrid retrieval + agentic search**。如果这是清华 Rust Agent 项目的话，我觉得这几个改动也比较容易形成明显的创新点。


分析当前架构，你的检索系统设计思路清晰（FTS5 + 分词预处理 + 范围过滤 + OR 降级），针对已知限制与性能瓶颈，提供以下针对性的改进建议，按实施优先级与回报率排序：

### 一、 分词与查询构建优化（低成本，即刻见效）

1. **修正分词过滤逻辑**
* **问题**：`chars().count() > 1` 会直接剔除所有单字关键词。在技术/编程领域，大量关键概念为单字（如“堆”、“栈”、“值”、“类”、“C”、“Go”）。搜“堆和栈的区别”时，“堆”与“栈”会被过滤掉。
* **建议**：修改过滤规则，保留汉字（`\p{Unified_Ideograph}`）单字，仅过滤纯标点、无意义停用词及单字母非保留字符。


2. **解决 OR 降级 $N$ 次 COUNT 查询的性能开销**
* **问题**：OR 降级对每个 term 执行一次 SQL 统计命中词数，带来 $O(N)$ 次数据库 IO。
* **建议**：
* **替代方案 A**：直接依赖 FTS5 原生的 `bm25()`。BM25 算法本身已将“词频”与“包含该词的文档数（IDF）”纳入权重计算，同时包含多个关键词的文档在 BM25 评分中天然高于只命中单个词的文档，无需手动执行 $N$ 次 `COUNT`。
* **替代方案 B**：若必须严格按“命中词数”主排序，可在单条 SQL 中通过位运算或 `matchinfo(notes_fts, 'pcx')` 辅助函数提取匹配情况，在 SQLite 内部单次查询完成。





---

### 二、 正文截取与 Snippet 动态窗口（解决核心体验痛点）

1. **Rust 侧动态切片（解决“命中词在 5000 字处被截断”问题）**
* **问题**：从头部固定截取 2000 字，极易丢失真正的命中文本。
* **建议**：
* 查出 `n.content` 原文后，在 Rust 侧对检索词 `terms` 做字符串匹配（如查找 `terms` 在 `n.content` 中的首次/高频出现位置 `pos`）。
* 以 `pos` 为中心向前后各扩展 300~500 字，截取上下文窗口（例如取 `[pos - 200, pos + 800]`），拼接 Markdown 标题后作为预览内容返回。
* 若一篇笔记存在多个分散的命中词，可提取多个短 Chunk（如 3 个 200 字的窗口），用 `...` 连接。




2. **动态 Token 预算管理**
* **问题**：固定 8 条 × 2000 字最多占据 16,000 字，显著增加 LLM 上下文负担与首字延迟（TTFT）。
* **建议**：
* 高相关性（AND 命中前排）：返回较完整的上下文窗口（如 800~1000 字）。
* 低相关性（OR 降级后排）：仅返回核心片段（200~300 字）。
* 控制单次工具调用的总字符数在 4000~6000 字以内。





---

### 三、 联动已有数据（利用 `concepts` 概念表）

1. **概念表预匹配与扩展（Query Expansion / Boosting）**
* **问题**：338 个概念未参与全文检索，造成数据孤岛。
* **建议**：
* **查询时**：接收到 query 后，先精确/模糊匹配 `concepts` 表。如果 query 命中某个概念名（如“所有权”），直接拉取该概念绑定的笔记列表（或直接提升包含该概念标记的笔记权重）。
* **入库/索引时**：在构建 `notes_fts` 时，将笔记关联的概念名称附加在 `notes_fts` 待索引文本的头部或单独字段中（例如权重高于正文的字段），提高概念词的 BM25 匹配度。





---

### 四、 Agent 交互与多轮检索策略

1. **放宽“单轮对话仅允许 1 次检索”的硬限制**
* **问题**：一次检索若未找到有效信息（如 0 hits 或全为 OR 降级低分结果），模型无法修正关键词重试。
* **建议**：将限制更改为条件分支：
* 若第 1 次检索结果的命中数等于 0，或最高 BM25 得分低于阈值，允许 Agent 重新生成关键词并进行第 2 次检索。
* 若第 1 次检索命中数足够且相关性高，禁止第 2 次检索。




2. **同义词与关键词改写（系统提示词层）**
* 在 Agent 系统提示词中增加要求：“提取 2-3 个核心实体/关键词进行检索，避免直接使用用户原句作为检索词（如将‘什么是所有权’提取为‘所有权’）”。



---

### 五、 中长期演进路线（解决语义理解缺失）

若需解决“内存管理”与“存储器管理”等近义词命中问题，可考虑以下逐步演进路线：

1. **同义词词典（成本最低）**：维护轻量级技术词汇同义词映射表，查询分词阶段自动扩展 OR 条件（如 `"内存管理" OR "存储器管理"`）。
2. **本地混合检索（Hybrid Search with RRF）**：
* 使用轻量级本地 Embeddings 模型（如 `bge-small-zh-v1.5`，通过 Rust ONNX Runtime 运行）。
* FTS5 负责精确技术名词匹配，Vector 负责语义泛化。
* 使用 RRF（Reciprocal Rank Fusion）合并两组 Top-K 结果。

这份检索策略已经相当完整，从分词、FTS5 查询到 Agent 防重都有清晰的设计。结合你列出的 6 个已知限制，我从 **「投入小、见效快」到「架构级改造」** 分三层给你建议：

---

## 一、短期优化（1-2 周，改动小）

### 1. 让概念表真正参与检索
**现状**：concepts 表有 338 个概念，但 `search_notes` 只查 `notes.content`，概念成了"摆设"。  
**建议**：在 `search::run` 里增加**概念别名扩展**：

```rust
// 查询前，先用 concepts 表做同义词/上位词扩展
let expanded_terms = expand_with_concepts(terms); 
// "所有权" → ["所有权", "ownership", "move语义", "借用检查"]
```

实现方式：
- 维护 `concept_aliases` 表（概念 ↔ 别名）
- 查询分词后，把命中的概念及其别名加入 `terms`
- OR 降级时这些扩展词也能参与命中词数统计

**收益**：解决限制 #3，顺带缓解 #2（无语义检索）的一部分。

---

### 2. 真正的 Snippet 提取（替代固定 2000 字预览）
**现状**：限制 #1 和 #5 —— 固定取前 2000 字，命中词可能在 5000 字处，LLM 完全看不到上下文。  
**建议**：不用 FTS5 的 `snippet()`（确实可读性差），而是**在原始 content 中定位命中词位置，提取周围上下文**：

```rust
fn extract_snippets(content: &str, terms: &[String], max_chars: usize) -> String {
    // 1. 在原始 content 中找所有 term 出现位置
    // 2. 合并重叠区域
    // 3. 每个命中区域提取前后 N 个字符，用 "..." 连接
}
```

**具体策略**：
- 如果笔记短（<2000 字）：全量返回
- 如果笔记长：返回 **3-5 个命中片段**，每个片段 300-500 字，用 `...` 拼接
- 在片段前后标注 `[命中]` 提示 LLM

**收益**：解决限制 #1 和 #5，大幅提升 LLM 回答质量。

---

### 3. 优化 OR 降级的 N 次 COUNT 查询
**现状**：限制 #4 —— 10 个词就要查 10 次。  
**建议**：用**单次 `matchinfo()` 查询**替代 N 次 COUNT：

```sql
SELECT n.id, n.title, matchinfo(notes_fts, 'pcnalx') as mi
FROM notes_fts JOIN notes n ON n.id = notes_fts.rowid
WHERE notes_fts MATCH '"所有权" OR "借用" OR "规则"'
```

`matchinfo` 返回的二进制数据里包含了每个词在每篇文档的命中次数，Rust 端解析即可一次性算出 `hit_terms` 和排序。  
**收益**：解决限制 #4，长查询从 O(N) 降到 O(1) SQL 调用。

---

### 4. 预览长度动态化
**现状**：固定 2000 字，短笔记浪费 token，长笔记信息不足。  
**建议**：
- 短笔记（<1500 字）：返回全文
- 中笔记（1500-4000 字）：返回 1500 字，但优先包含命中片段
- 长笔记（>4000 字）：返回 3 个 snippet + 目录结构

---

## 二、中期优化（1 个月，需要一定重构）

### 5. 查询意图理解与重构（Pre-retrieval）
**现状**：用户问"什么是所有权"，直接拿这 5 个字去搜。但 FTS5 是关键词匹配，问法一变（如"Rust 里变量归谁管"）就搜不到。  
**建议**：在 `Agent Loop` 里增加一个轻量步骤——**查询重写（Query Rewriting）**：

```
用户提问 → LLM 生成检索关键词（1-3 个）→ search_notes
```

让 LLM 把口语化问题翻译成笔记中更可能出现的术语。  
**示例**：
- "变量归谁管" → `["所有权", "ownership", "变量绑定"]`
- "怎么传参不拷贝" → `["借用", "引用", "move"]`

这可以在 system prompt 里直接做，不需要额外工具调用。

---

### 6. 引入 BM25 分数阈值，减少噪声
**现状**：OR 降级时，只要命中一个词就参与排序，可能引入大量低相关文档。  
**建议**：
- AND 查询：直接用 bm25，信任 FTS5
- OR 降级：设置 `bm25 < threshold` 或 `hit_terms >= 2` 的硬门槛
- 或者引入 **RRF（Reciprocal Rank Fusion）**：`score = 1/(k + bm25_rank) + α * hit_terms`

---

### 7. 课程权重与笔记重要性
**现状**：排序只看 bm25 和命中词数。  
**建议**：给排序加入额外信号：
- **课程匹配度**：当前课程笔记权重 ×1.5，跨课程 ×1.0，未归类 ×0.8
- **笔记类型**：概念解释页 > 代码示例 > 杂记
- **引用次数**：如果笔记 A 被很多其他笔记 wikilink 引用，权重提升

这些可以作为 `notes` 表的额外列，在排序时作为次排序键。

---

### 8. 放宽 Agent 防重策略
**现状**：限制 #6 —— 每轮对话只能搜一次，LLM 无法用不同关键词做多轮检索。  
**建议**：把"硬性限制 1 次"改为**"软性预算制"**：
- 每轮对话允许 2-3 次 `search_notes` 调用
- 但要求每次查询的 `terms` 必须有显著差异（Jaccard 相似度 < 0.5）
- 或者：第一次搜不到结果时，自动触发第二次（用扩展词）

---

## 三、长期架构升级（2-3 个月，可选）

### 9. 混合检索：FTS5 + 向量检索（RAG 2.0）
**现状**：限制 #2 —— 纯关键词，无法理解语义。  
**建议**：引入轻量级向量检索作为第二路召回：

```
查询 → 并行执行：
  ├─ 路径 A：FTS5 关键词检索（精确匹配，高置信）
  └─ 路径 B：Embedding 相似度检索（语义匹配，召回同义词）
              ↓
         合并结果（RRF 融合）→ 去重 → top_k
```

技术选型：
- Embedding 模型：`BAAI/bge-small-zh` 或 `text-embedding-3-small`（轻量、中文好）
- 向量库：`sqlite-vss`（SQLite 插件，零额外运维）或 `pgvector`（如果愿意切 Postgres）

**注意**：这不是替换 FTS5，而是**补充**。FTS5 对精确术语（如 `Box::leak`）的匹配仍然不可替代。

---

### 10. 检索结果的后排序（Re-ranking）
在 FTS5/向量召回 top 50 后，用一个轻量 Cross-Encoder（如 `bge-reranker`）做精排，取 top 8 给 LLM。  
**收益**：大幅提升相关度，但增加一次模型调用延迟。

---

### 11. 用户反馈闭环
增加 👍/👎 按钮，记录"哪些检索结果最终帮助 LLM 生成了好答案"。长期可用这些信号训练或微调排序权重。

---

## 优先级总结

| 优先级 | 改进项 | 解决限制 | 预估工作量 |
|--------|--------|----------|------------|
| 🔴 P0 | 概念表参与检索 | #3, #2 | 2-3 天 |
| 🔴 P0 | Snippet 提取替代固定预览 | #1, #5 | 3-5 天 |
| 🟡 P1 | matchinfo 替代 N 次 COUNT | #4 | 2-3 天 |
| 🟡 P1 | 查询重写（Pre-retrieval） | #2 | 1-2 天（prompt 工程）|
| 🟢 P2 | 动态预览长度 + 课程权重 | #5 | 3-4 天 |
| 🟢 P2 | 放宽 Agent 防重 | #6 | 1-2 天 |
| ⚪ P3 | 向量检索混合架构 | #2 | 2-4 周 |
| ⚪ P3 | Re-ranking + 反馈闭环 | — | 1-2 月 |

---

**我的建议**：先集中做 P0 的三件事（概念扩展、Snippet、matchinfo 优化），它们改动范围都在现有 `storage` 和 `tools` crate 内，不需要动 Agent 架构，但能让检索质量上一个台阶。向量检索可以等数据量或用户量上来后再考虑。

需要我针对其中某一项展开详细实现方案吗？