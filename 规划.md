# Rust 大作业规划：个人知识库 Agent

## 一、项目定位

> **"Agent 主动管理你的知识，而不是被动回答你的知识。"**

- 名字建议：`mynotes-agent` / `SecondBrain.rs`
- 一句话介绍：一个用 Rust 实现的本地优先个人知识管理 Agent，自动导入、分类、关联你的笔记/论文/代码，并主动发现知识盲区、生成复习计划。
- 形态：OpenCode 风格 **TUI**（ratatui + crossterm），本地 SQLite 存储。

### 差异化定位

- 真实痛点：笔记散落 Markdown/PDF/Git 仓库，读完就忘，ChatGPT 无法访问本地知识
- 核心定位强调"**主动管理知识的学习闭环**"（导入消化复习三模式），而非"本地笔记 RAG 问答"
- 与现有方案对比：Obsidian + Copilot 插件、Notion AI、ChatPDF 类均偏被动问答，缺少导入→提取概念→归类→复习的全流程主动管理

---

## 二、三种模式设计（产品主线）

> 对应知识生命周期：**输入(导入) → 消化(学习) → 巩固(复习)**

### 模式 1：导入 (Import)
- 批量导入本地文件夹的 md/pdf/pptx/图片
- LLM 自动归类到课程（可手动指定），提取概念
- 导入过程有进度条、可中断（R4 的真实使用场景）

#### 导入流水线实现方案

**① 收集文件**：`/import ~/my-notes --course rust`，用 `walkdir` 递归遍历，按扩展名分类（md/txt、pdf、pptx、图片），生成 `ImportJob { files, course }`。

### 真实语料（导入测试数据）

本仓库自带两门课的真实资料，是 M0/M6/M7 与 Demo 的标准测试集：

- `程序设计实践-rust/note/`：3 讲课程笔记（中文 Markdown）
- `CSAPP/notes/`：17 篇章节/实验笔记（中文 Markdown）
- `程序设计实践-rust/resources/`：3 个课件 **PDF**（M0 pymupdf 验证 + pdf 导入测试）
- `CSAPP/resources/*.pptx`：约 17 讲课件 **PPTX**（pptx 文字抽取测试）

解析器必须处理的实际特征：

- **Obsidian wikilink**：`[[#标题|显示文本]]`、`[[路径|别名]]`——正文入库前必须剥离为纯文本（保留显示文本），否则污染 FTS 索引与概念抽取
- **头部元信息引用块**（来源/整理日期）与 **"目录"小节**：对检索属噪声，FTS 内容应跳过目录段
- **标题层级即概念边界**：`## 移动语义 (Move Semantics)` 这类二级标题是现成的概念候选，可作为 LLM 抽取结果的校验参照
- **文件名含空格与括号**：如 `17-processes.pptx` 与 `17-processes (1).pptx`——路径处理不能按空白切分；`(1)` 副本内容相同，正好用于验证 content_hash 幂等去重

Demo 的跨课程问答（ownership vs 虚拟内存）直接用这两门课演示。

**② 任务化 + 进度上报（复用 R4 事件通道）**：`tokio::spawn` 导入任务，逐文件串行处理（LLM 调用有速率和费用），通过 mpsc 向 TUI 发事件：

```rust
enum ImportEvent {
    FileStart { name: String },
    FileDone  { name: String, concepts: usize },
    FileFail  { name: String, err: String },
    Finished  { ok: usize, fail: usize },
}
```

TUI 渲染 `[12/37] ▓▓▓░░ lecture03.pdf → os 课程，8 个概念`；每个文件开始前检查 CancellationToken，Ctrl+C 随时可停。

**③ 格式解析器（enum 分发）**：统一产出 `RawDoc { title, text }`，后续流程对格式无感。

| 类型 | 方法 |
|-|-|
| md/txt | `tokio::fs::read_to_string`；清洗规则（wikilink 剥离、目录段剔除）见"真实语料"一节 |
| pdf | **pymupdf** 提文本：Rust 经 `tokio::process::Command` 调用自带的 `pdf_extract.py` 辅助脚本（stdout 输出 JSON：页数 + 逐页文本），天然支持按页进度上报（R4）；同步等待丢 `spawn_blocking`。降级链：提取失败/空文本 → 标记疑似扫描版 → 可选走多模态识图（顺带展示 R6 成本统计）→ 最终失败仅存元数据并提示安装方式 |
| pptx | `zip` 解压 → 读 `ppt/slides/slide*.xml` → `quick-xml` 抽 `<a:t>` 标签文本按页拼接 |
| 图片 | base64 → 多模态模型转文字描述，返回文本作为正文 |

**④ LLM 归类与概念抽取（JSON mode）**：每篇调一次 LLM 结构化输出 `{title, course, summary, concepts[{name}]}`；已指定 course 则跳过归类；`course` 可为 null（不属于任何课程时落入 all 区，不强行归类）；解析失败重试一次，再失败只存原文。
**JSON mode 兼容性降级**：并非所有 OpenAI-compatible endpoint 都支持 `response_format: json_object`。降级链：不支持/报错 → prompt 里要求"只输出 JSON" → 从回复文本正则提取第一个完整 `{...}` 块解析 → 再失败走已有的重试一次后跳过逻辑。出题、批改、大纲生成共用这条降级链。

**⑤ 入库 + 容错**：notes 存全文 + FTS5 索引；concepts 按 `(name, course)` 去重；按内容 hash 幂等去重（重复导入跳过）；单文件失败不中断，最后汇总"成功 n / 失败 m"。

### 模式 2：学习 (Study)
- **知识问答**：检索知识库后回答，附引用来源
- **课程大纲梳理**：对指定课程生成结构化大纲（LLM 基于该课程全部笔记归纳章节/知识点）

#### 学习模式实现方案：Agent Loop + RAG 工具集

不写死检索流程，把检索能力做成工具交给 Agent 自己调度。System prompt 注入分区上下文（`当前课程: rust | 可切换: os, all`）。简单问题 LLM 可直接答，复杂问题多轮检索——体现 agent loop 相比固定管道的价值。

**知识问答检索管线（search_notes 内部）**：

1. 第一跳：FTS5 全文检索当前课程（含 all 区）notes，取 top-k 片段。
   **中文分词方案 = jieba-rs + FTS5 默认 tokenizer**：入库与查询前都用 jieba 切词、空格连接后写入/查询（notes 表另存原文，展示引用时取原文片段）。方案取舍与实现细则——为什么不用 trigram/unicode61、MATCH 词加双引号、AND 过严降级 OR 并按"命中词数 + bm25"排序、命中词数需自行统计——**统一见附录 A 决策 D1**。
2. 检索范围 = 当前课程 ∪ all 区，一条 WHERE 条件即可；用户想跨课程提问时切到 all 或在问题中直接提及另一课程主题，FTS5 均可命中
3. 上下文组装：片段带来源标号 `[1] lecture03.pdf (os)`；设 token 预算上限（约 4k），超了按相关度截断。
4. 回答约束：prompt 要求"仅依据资料回答，用 [n] 标注引用"，TUI 把引用渲染成底部脚注列表——引用来源是结构保证的，不靠模型自觉。

**大纲梳理（generate_outline）**：

1. 取该课程全部 notes 的 summary + concepts（导入时已生成，不重读全文）
2. JSON mode 输出层级大纲：
   ```json
   { "sections": [
       { "title": "内存管理",
         "points": ["虚拟内存", "页面置换"],
         "refs": [3, 7] } ]
   }
   ```
3. TUI 渲染缩进树，`/outline <course> --export` 导出 Markdown；summary 总量超上下文时降级为 map-reduce（分批归纳小纲再合并）

**大纲呈现（两层）**：

- V1（保底版本）：聊天流内渲染缩进树（ratatui 多行 Text + Style，标题加粗、层级缩进、每节标注笔记数），作为特殊消息存入会话流，重启后随历史还原；`/outline rust --export` 导出 Markdown
- V2（缓冲期可选）：交互式大纲视图——主区域临时切换，光标选中章节：`Enter` 查看引用的笔记/打开原文，`a` 以该章节 refs 为上下文直接提问（免检索），`q` 返回聊天流。把大纲从展示物升级为导航入口

**会话细节**：学习模式对话照常进 sessions 表（R5），每个会话记录当时的 course 分区，恢复会话时连分区一起还原。

### 模式 3：复习 (Review)
- 用户指定某个知识板块（课程/概念），AI 出题
- 题型：简答题、选择题（JSON 结构化存库）
- 答完后 AI 对照笔记原文批改，标记薄弱概念 → 反哺主动学习引擎

#### 复习模式实现方案

**① 出题（create_review）**：

入口 `/review rust 所有权` 或 `/review all --n 10`。收集指定范围的 notes summary + concepts + 掌握度数据，LLM JSON mode 生成题目集：

```json
{ "questions": [
    { "type": "choice", "question": "...",
      "options": ["A","B","C","D"], "answer": 2,
      "explanation": "...", "concept_id": 12 },
    { "type": "short_answer", "question": "...",
      "key_points": ["..."], "concept_id": 15 } ]
}
```

prompt 必须附带该范围的真实笔记资料，保证题目出自用户学过的内容。题目存 `quizzes`/`questions` 表，可重做。

**② 答题（TUI 复习视图）**：

- 选择题：数字键 1-4 直接作答，本地比对答案立即反馈（不走 LLM，零成本零延迟）
- 简答题：文本框输入，提交后批改
- 显示进度 `[3/5]`，Esc 可中断退出（R4）

**③ 批改（grade_answer，仅简答题）**：

将 用户答案 + 对应笔记原文片段 + key_points 发给 LLM，JSON 返回：

```json
{ "score": 70, "missing": ["缺页面置换的触发条件"], 
  "comment": "..." }
```

TUI 展示得分、缺失要点、笔记出处引用。

**④ 掌握度闭环（连接定制点②的枢纽）**：

`concept_mastery(concept_id, attempts, correct, last_reviewed)` 表记录每题结果：
- 答错的概念 → 标记薄弱 → 主动学习引擎启动时优先推送
- 下次 `/review` 出题时，把掌握度低的概念的权重调高（简化版间隔重复）

**⑤ 复习形式（强度阶梯）**：

1. **闪卡**：正面概念/背面解释，用户自评会/不会，不会的进薄弱清单——数据结构与选择题共用，去掉批改环节，快速过大量知识点
2. **标准测验**：选择题 + 简答题（见①-④）

复习严格限定在课程范围内出题；跨课程关联只在学习模式问答中体现。

**⑥ 数据一致性**：复习会话照常存 sessions（R5）；批改用的"笔记原文"直接从 notes 表取，保证评分标准来自用户自己的资料而非模型常识。

### 课程分区 + all 全局区

- 数据层：`courses` 表；notes/concepts 带 `course_id`；特殊值 `all` 表示跨课程全局区
- **all 区持有所有课程的知识**：视图层面 = 全部课程的并集，不重复存储
- **未归类知识**：`course_id` 允许为 NULL。导入时 LLM 判断不属于任何课程（或不确定）则输出 `"course": null`，笔记落入 all 区，问答/检索正常可用；提供 `/move <note_id> <course>` 事后迁移；删除课程时其笔记回落 all 而非删除。查询语义统一：all = 不限课程过滤，NULL = 未归类，两者天然兼容

### 笔记管理：删除与移动

**删除 `/delete <note_id>`**：
- 只从知识库移除，绝不碰磁盘原文件（Agent 操作本地数据的安全红线）
- 级联清理：FTS5 索引行、该笔记独有的 concepts（被他笔记共用则保留）
- TUI 确认后执行；`/delete --course rust` 支持批量清空

**移动 `/move <note_id> <course>`**：
- 仅更新 `notes.course_id`，正文不动
- 概念迁移：目标课程已有同名概念则合并（mastery/usage 数据并入），否则改挂新课程——防止重复概念

**TUI 入口**：课程栏下展示笔记列表，选中后 `d` 删除 / `m` 移动（弹出课程选择菜单）

**note_id 查找方案**：三层共用一个 `resolve_note(query) -> NoteId` 函数（精确 id → 短 id → 标题模糊匹配）：
1. 默认交互是 TUI 列表选中，不需要输入 id
2. 命令支持标题模糊匹配：`/delete lecture03`、`/move "虚拟内存" os`；多命中列出候选、零命中报错
3. 笔记列表/搜索结果显示短 id 兜底（内容 hash 前 4 位），如 `[a3f2] lecture03.pdf (rust)`
- **跨课程支持**：all 区天然持有全部课程知识；课程内提问也可扩大检索范围至 all，无需额外机制

TUI 上用 `/course rust` / `/course all` 切换分区，状态栏显示当前课程。

### 命令与分区上下文：当前课程即默认作用域

所有接受课程参数的命令（/review、/outline、/import 等）共用一套解析规则 `resolve_scope(args)`：

- args 显式指定课程 → 用 args（临时跨区操作）
- 否则 → 继承当前分区（状态栏所见即命令作用域）

```
/course rust
/review              → 等价于 /review rust
/review 所有权       → 等价于 /review rust 所有权
/review os           → 显式参数优先
```

例外：all 区下无参 `/review` 弹出课程选择器强制选课（复习全部范围太大且出题质量差），记住上次选择作为默认光标；学习模式检索在 all 下则天然合理。

### Help 系统

- `/help`：按模式分组列出全部命令 + 一句话说明；`/help <cmd>` 显示完整参数与示例
- 首次启动在聊天区顶部提示 `输入 /help 查看可用命令`
- 命令解析失败时自动附上对应用法说明（错误信息即帮助）
- 实现为静态文本表，复用 clap 的参数定义

### 分类体系设计决策：课程是唯一分类维度

- **课程是唯一主分类**（notes 属于恰好一个课程）：核心用例"复习某门课/生成课程大纲"天然以课程为边界；LLM 自动归类到封闭课程列表远比开放生成标签稳定（避免 `#rust`/`#Rust语言` 式标签爆炸）；且课程分区本身就是区别于通用笔记工具的场景定制点
- **不做辅助标签**（已砍）：横切属性过滤（如 `--tag 考试重点`）使用频率低，而标签生成为每次导入增加输出 token 与解析失败面。若后续需要，可作为加分项补回（notes.tags JSON 数组，导入时顺手生成）
- **不做树状层级标签**：知识结构功能已由 concepts（含层级命名约定）承担，再做标签树属于重复建设。以上取舍写入设计文档作为技术论证

---

## 三、必做功能 R1-R6 落地方案

| 要求 | 实现 | 关键依赖 |
|-|-|-|
| R1 核心逻辑 Rust | 全部 Rust，LLM 只通过 HTTP API 调用 | tokio, reqwest |
| R2 用户界面 | TUI：固定 shell（左侧课程/会话栏 + 顶部状态栏显示当前课程/模式/token费用），主区域按模式切换视图——学习=聊天流、导入=进度队列、复习=题目卡片（选择题按键作答）。参考 OpenCode 但不照搬，UI 本身体现场景定制 | ratatui, crossterm |
| R3 模型配置 | `config.toml` 支持 provider 列表（endpoint/api_key/model/价格/上下文长度/**思考模式**），运行时可 `/model` 切换 | serde, toml |
| R4 实时进度 + 中断 | Agent Loop 通过 `tokio::sync::mpsc` 向 TUI 推送事件（工具调用、进度%）；Ctrl+C / Esc 发送 CancellationToken 中断 | tokio-util |
| R5 历史上下文 | Session 表存完整 messages/tool_calls/results；TUI 左侧会话列表可切换/恢复；另提供 `/export <session>` 把完整上下文导出为 JSON 文件；执行过程的事件轨迹在 TUI 中可见，非黑盒 | rusqlite 或 sqlx + sqlite |
| R6 Token 统计 | 从 API response 的 usage 字段累计 prompt/completion tokens，按配置价格算 cost；**统计覆盖所有外部 AI 调用**（chat、多模态识图、可选 embedding）；TUI 底部状态栏实时显示；支持 `max_cost` 预算熔断。注意：请求进行中无法取消已产生的 token 计费，熔断语义定义为"**发起下一个请求前检查预算，超则拒绝并提示**" | - |

---

## 四、系统架构

```
mynotes-agent/            (cargo workspace)
├── crates/
│   ├── core/             # Agent Loop、Tool trait、Provider trait、事件总线
│   ├── providers/        # OpenAI-compatible LLM 客户端 (reqwest)
│   ├── tools/            # 具体工具实现
│   ├── storage/          # sqlite: sessions, notes, concepts, quizzes, concept_mastery, usage
│   └── tui/              # ratatui 界面 (bin crate)：App shell + 按 Import/Study/Review 分发的 mode view
├── config.toml
└── README.md
```

### 数据库 Schema（DDL 定稿基线）

> schema 变更规则：先改这里的 DDL，再写迁移；禁止各处散落 CREATE TABLE。

```sql
PRAGMA journal_mode = WAL;
PRAGMA foreign_keys = ON;

-- all 区不是一行记录：course_id IS NULL 即未归类/all，查询不过滤课程即为 all
CREATE TABLE courses(
  id   INTEGER PRIMARY KEY,
  name TEXT UNIQUE NOT NULL               -- 如 rust / csapp
);

CREATE TABLE notes(
  id           INTEGER PRIMARY KEY,
  course_id    INTEGER REFERENCES courses(id) ON DELETE SET NULL,
  title        TEXT NOT NULL,
  source_path  TEXT,                      -- 原始文件路径（只读参考，绝不回写）
  content      TEXT NOT NULL,             -- 清洗后原文（wikilink 已剥离）
  content_hash TEXT UNIQUE NOT NULL,      -- sha256 前 16 位：幂等去重 + 短 id
  created_at   TEXT NOT NULL DEFAULT (datetime('now'))
);

-- rowid = notes.id；content 存 jieba 预分词文本（决策 D1）
CREATE VIRTUAL TABLE notes_fts USING fts5(content);

CREATE TABLE concepts(
  id        INTEGER PRIMARY KEY,
  name      TEXT NOT NULL,
  course_id INTEGER REFERENCES courses(id) ON DELETE SET NULL,
  UNIQUE(name, course_id)
);

CREATE TABLE note_concepts(               -- 笔记↔概念多对多：共用概念不重复存
  note_id    INTEGER REFERENCES notes(id)    ON DELETE CASCADE,
  concept_id INTEGER REFERENCES concepts(id) ON DELETE CASCADE,
  PRIMARY KEY(note_id, concept_id)
);

CREATE TABLE quizzes(
  id         INTEGER PRIMARY KEY,
  course_id  INTEGER REFERENCES courses(id),
  scope      TEXT NOT NULL,               -- 出题范围描述，如 "rust 所有权"
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE TABLE questions(
  id              INTEGER PRIMARY KEY,
  quiz_id         INTEGER REFERENCES quizzes(id),
  type            TEXT NOT NULL CHECK(type IN ('choice','short_answer')),
  question        TEXT NOT NULL,
  options_json    TEXT,                 -- 选择题选项 JSON 数组
  answer          INTEGER,              -- 选择题正确下标
  key_points_json TEXT,                 -- 简答题要点 JSON 数组
  explanation     TEXT,
  concept_id      INTEGER REFERENCES concepts(id)
);

CREATE TABLE attempts(                    -- 每次作答记录（可重做）
  id           INTEGER PRIMARY KEY,
  question_id  INTEGER REFERENCES questions(id),
  answer_text  TEXT,
  score        INTEGER,                   -- 仅简答题有
  missing_json TEXT,
  created_at   TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE TABLE concept_mastery(
  concept_id    INTEGER PRIMARY KEY REFERENCES concepts(id),
  attempts      INTEGER NOT NULL DEFAULT 0,
  correct       INTEGER NOT NULL DEFAULT 0,
  last_reviewed TEXT
);

CREATE TABLE sessions(
  id         INTEGER PRIMARY KEY,
  title      TEXT,
  course_id  INTEGER REFERENCES courses(id),  -- 会话发生时的分区，恢复时还原
  created_at TEXT NOT NULL DEFAULT (datetime('now'))
);

CREATE TABLE messages(
  id         INTEGER PRIMARY KEY,
  session_id INTEGER REFERENCES sessions(id) ON DELETE CASCADE,
  role       TEXT NOT NULL CHECK(role IN ('system','user','assistant','tool')),
  content    TEXT NOT NULL,               -- 原样 JSON 字符串（含 tool_calls / tool_call_id）
  seq        INTEGER NOT NULL             -- 会话内顺序
);

CREATE TABLE usage_log(                   -- R6：覆盖 chat/vision/embedding 全部外部调用
  id                INTEGER PRIMARY KEY,
  provider          TEXT NOT NULL,
  model             TEXT NOT NULL,
  kind              TEXT NOT NULL,        -- chat | vision | embedding | review | outline ...
  prompt_tokens     INTEGER NOT NULL,
  completion_tokens INTEGER NOT NULL,
  cost              REAL NOT NULL,        -- 按配置单价折算
  created_at        TEXT NOT NULL DEFAULT (datetime('now'))
);
```

### config.toml 示例（R3 定稿格式）

```toml
default_provider = "deepseek"
max_cost = 5.0                            # 预算熔断阈值（元）

[[providers]]
name = "deepseek"
endpoint = "https://api.deepseek.com/v1"
api_key_env = "DEEPSEEK_API_KEY"          # 优先环境变量引用，避免明文入库
model = "deepseek-reasoner"
price_prompt = 4.0                        # 元 / 百万 token
price_completion = 16.0
context_length = 65536
thinking = true                           # 思考模式开关

[[providers]]
name = "openai-compatible"
endpoint = "https://example.com/v1"
api_key = "sk-..."                        # 也允许明文（本地文件）
model = "gpt-4o-mini"
price_prompt = 1.0
price_completion = 4.0
context_length = 128000
thinking = false
```

### 存储层并发设计

选定方案见附录 A 决策 D2：**storage 保持同步 API（rusqlite + bundled FTS5），所有调用点经 `tokio::task::spawn_blocking` 进入 blocking 线程池**；PDF 子进程等待同理。备选方案仅在实测出性能问题时升级。

### 核心 trait 设计

```rust
#[async_trait]
pub trait Tool: Send + Sync {
    fn name(&self) -> &str;
    fn description(&self) -> &str;
    fn schema(&self) -> Value;                       // JSON Schema 给 LLM
    async fn execute(&self, args: Value) -> Result<Value>;
}

#[async_trait]
pub trait Provider: Send + Sync {
    async fn chat(&self, msgs: &[Message], tools: &[Value]) -> Result<Response>; // 含 usage
}
```

**Provider trait 的第二个实现：MockProvider（可测试性即抽象的理由）**。测试绝不打真实 LLM API（慢、花钱、结果不确定）。做法：`MockProvider { responses: Vec<Response> }` 按序返回预置响应；用真实 API 录制响应存成 JSON fixture，测试时回放。导入流水线、agent loop、批改逻辑因此可离线测全流程——这也是设计文档里"为什么要有 Provider trait 抽象"的现成论据。

### 工具与命令的边界（重要设计决策）

**命令直连内部函数，不进 agent loop**：`/import`、`/review`、`/delete`、`/move` 这类批量或费用敏感操作由命令处理器直接调用流水线函数——不让 LLM 自主决定是否批量读文件、是否烧 token，可靠且可测试。**只有只读检索型工具暴露给 agent loop 自主调度**（学习模式问答时 LLM 自己决定查不查、查几轮）：

| Agent Tool | 功能 |
|-|-|
| `search_notes` | jieba + FTS5 检索笔记与概念，范围=当前课程∪all（合并原 search_course） |
| `list_courses` | 列出课程及各课程笔记/概念统计 |
| `generate_outline` | 基于课程内全部 notes 的 summary/concepts 生成结构化大纲（学习模式） |

内部流水线函数（命令直连，非 agent 工具）：import_file、extract_concepts、create_review、grade_answer——场景定制①的知识生命周期流水线即由它们构成。

### 场景定制 ②：主动学习引擎

- 后台任务（tokio spawn）：扫描近期会话/导入内容 → 统计概念出现与提问频次 → 找出"导入但从未被问过/复习过"的知识点 → 生成复习任务（可按课程维度出题，如"帮我复习 Rust 课这周的知识点"）。
- TUI 启动时展示 "今日复习建议"，可一键让 Agent 出题测试。

### Memory 分层（参考 Letta/Mem0 思想，简化实现）

1. Working Memory：当前 session 的 messages
2. Episodic Memory：sessions 表（历史任务）
3. Semantic Memory：concepts 表（长期知识结构）

向量检索可选加分项：sqlite 存 embedding + 余弦相似度（不引入重型向量库，时间不够直接砍掉，FTS5 保底）。

### 测试策略（随里程碑走，不堆在打磨期）

> 测试的核心目的：防回归（改动易悄悄改坏别处），保证质量可验证。

- **每个里程碑收尾时补该模块的纯逻辑单测**（随手写边际成本低，拖到打磨期反而要重新理解代码）：
  - M3：jieba 分词管线一致性（入库/查询同一切词）、FTS5 查询构造（引号包裹、AND→OR 降级）
  - M1/M2：MockProvider 注入下的 agent loop 行为、tool call 解析
  - M6–M8：LLM JSON 输出容错（畸形输出/markdown 包裹 fixture）、`resolve_note` 三层匹配、`resolve_scope` 继承、cost 计算器
- **不值得自动化测**：TUI（手动验收手感）、真实 API 集成（用 MockProvider + 录制 fixture 替代）
- **打磨阶段只做**：集成测试串测（导入→检索→出题全链路走 MockProvider）、查漏补缺

---

## 五、里程碑规划（按顺序推进，不绑定具体日期）

> 两条推进原则：① **每个里程碑验收真实效果**——中文 PDF 提取、jieba 检索召回、LLM 归类/出题质量、TUI 手感都是效果问题，编译通过 ≠ 完成；② **15 分钟"能讲清楚"自查**——核心模块的实现细节与设计理由要能脱稿讲解。不要单会话从头跑到尾：按里程碑推进正是为了防最后集成爆炸。
>
> **推进规则**：里程碑严格按序完成（M2 依赖 M1 的 Provider），但不设每日 deadline——某个里程碑超时，就从后面的可砍项里扣（见文末缓冲策略），而不是压缩验收环节。

### 开发里程碑

| # | 目标 | 验收产出（命令 + 预期结果） |
|-|-|-|
| M0 | **风险前置验证**（半天内）：① 用 `程序设计实践-rust/resources/01-basic.pdf` 实测 pymupdf 子进程方案——验证点：Windows 下 `python` vs `py -3`、子进程显式 `PYTHONIOENCODING=utf-8`（默认 GBK 会乱码）、脚本路径定位方式；② rusqlite `bundled` feature 下 FTS5 + jieba 中文检索实验；③ 目标 provider 实测一次流式响应与思考模式字段结构（`reasoning_content` vs `reasoning` 各家不一） | 三份结论写进附录 A 对应决策；pdf_extract.py 对真实课件输出合法 UTF-8 JSON；FTS5+jieba 实验代码留在 `crates/storage/tests/` |
| M1 | 项目骨架：workspace + config.toml 解析 + Provider trait + OpenAI-compatible 客户端 | `cargo run -p providers --example once -- "你好"` 打印回复与 prompt/completion tokens 及折算 cost |
| M2 | Agent Loop：Tool trait、tool call 解析执行、循环直到最终答案 | `cargo run -p core --example loop-demo`：MockProvider 驱动，日志可见完整 tool call 往返；`cargo test -p core` 绿 |
| M3 | Storage：SQLite schema（按第四章 DDL）+ FTS5(jieba 预分词) | `cargo test -p storage` 全绿（含 session roundtrip 含 tool_calls 原样还原、中文检索用例） |
| M4 | TUI v1 + 事件通道骨架：App shell（侧栏+状态栏）、聊天流、mpsc 事件通道、CancellationToken 中断 | 手动验收（唯一手测项）：TUI 里能完整聊天、Ctrl+C 可中断且终端状态恢复；`cargo clippy --workspace -- -D warnings` 清零 |
| M5 | 补齐基础要求：`/model` 切换 + 思考模式配置、session 导出 JSON、成本统计完善 | `/export` 导出的 JSON 重启后可加载还原；usage_log 覆盖一次会话全部请求；**R1-R6 全部打通 ✅** |
| M6 | 定制点①：导入流水线 + 课程分类（md/pdf/pptx/图片） | 导入本仓库全部真实语料：`/import ./程序设计实践-rust --course rust`（md+pdf）与 `/import ./CSAPP --course csapp`（md+pptx，含 `(1)` 重复副本）后，sqlite3 查询：notes 无 content_hash 重复、concepts>0、wikilink 已剥离、pptx/pdf 正文非空；进度条可 Ctrl+C 中断 |
| M7 | 检索 RAG（jieba 分词管线，范围=当前课程∪all）+ generate_outline 课程大纲 | `/course rust` 问"什么是所有权"回答带 [n] 引用并命中《02-所有权与结构化数据》片段；all 下跨课程问 ownership vs 虚拟内存命中两门课；`/outline rust` 输出含 refs 的缩进树并可导出 Markdown |
| M8 | 定制点②：复习模式（create_review 出题 + grade_answer 批改 + 标准测验）+ 掌握度闭环 | `/review rust 所有权` 出题作答后 attempts 表有记录、简答题返回 score/missing、concept_mastery 行被更新 |

### 打磨阶段（M8 之后，按剩余时间取用）

- 错误处理、clippy 清零；单测已随里程碑补齐（见"测试策略"），本阶段做集成测试串测与查漏
- README（含 pymupdf/PEP 668 安装说明）、Windows 兼容性抽查（至少跑通导入）
- 设计文档 PDF、Demo 演练
- 加分项按序取用：闪卡 → 主动学习引擎完整版 → 大纲交互视图 V2 → 扫描版 PDF 多模态识图

缓冲策略：主动学习引擎来不及就降级为"启动时统计报告"（仍是定制点②最小版本）；闪卡和大纲交互视图 V2 为第一顺位可砍项；扫描版 PDF 的多模态识图降级路径仅在时间富余时接。

### Demo 剧本

> 每一幕都有降级版本，可砍项被砍后 Demo 依然完整。

1. 导入模式：导入一个真实课程文件夹 → 展示进度条、自动归类、概念抽取
2. 学习模式：在 Rust 课下提问，答案引用笔记；问跨课程问题（ownership vs 虚拟内存）→ 展示跨课程关联检索；`/outline` 生成课程大纲
3. 复习模式：指定"Rust 所有权"板块出题作答（选择+简答）、AI 对照笔记批改标记薄弱点；〔若闪卡已实现：先闪卡快过再标准测验〕
4. 重启程序 → 展示历史会话加载 + 主动建议（完整引擎或降级版"启动时统计报告"均可演示）
5. 全程底部显示 token 与费用 → 顺手展示预算熔断

---

## 六、技术栈清单

```toml
tokio          # async runtime
reqwest        # LLM HTTP 客户端（开 stream feature）
eventsource-stream  # SSE 流式响应解析（聊天流式渲染必需）
futures-util   # Stream 处理
serde/serde_json/toml
rusqlite       # 存储（比 sqlx 轻，同步 API + spawn_blocking 调用）；想秀异步可用 sqlx
ratatui + crossterm  # TUI
async-trait    # Tool/Provider trait
tokio-util     # CancellationToken
anyhow/thiserror
clap           # CLI 参数
tracing        # 日志
walkdir        # 导入时递归遍历目录
jieba-rs       # 中文分词：入库预分词 + 查询分词，配合 FTS5（trigram 对 2 字中文词无效）
zip + quick-xml # pptx 文字抽取
pymupdf        # pdf 提文本（外部 Python 依赖，Rust 经子进程调 scripts/pdf_extract.py；跨平台 pip 安装；AGPL，文档注明）
base64         # 图片多模态输入
```

## 七、参考源码（按需看，别陷进去）

| 项目 | 看什么 | 时间预算 |
|-|-|-|
| sst/opencode | TUI 布局、session 管理、agent loop 结构 | 半天 |
| 0xPlaygrounds/rig | Tool trait / Provider 抽象写法 | 2 小时 |
| letta/Mem0 | memory 分层思想（只读博客/docs 即可） | 1 小时 |

## 八、风险预案

| 风险 | 对策 |
|-|-|
| TUI 开发超预期 | M4 先做单视图极简版，模式化布局后移；复习模式的卡片视图可降级为聊天流内出题 |
| LLM 输出不稳定导致概念抽取失败 | 用 JSON mode / 结构化输出 + 失败重试一次后跳过 |
| PDF 解析麻烦 | pymupdf 主路径（跨平台 pip wheel，逐页提取，质量经实测验证）；README 写清 Python 依赖及 Debian/Ubuntu PEP 668 的安装说明（venv / pipx / --break-system-packages）；失败降级为多模态识图或仅存元数据；pptx 用 zip+XML 抽文字；图片用多模态模型识图，OCR 不做 |
| AI 单会话连跑全程导致集成爆炸 | 坚持按里程碑推进，每步真实运行验收后再继续；每个里程碑花 15 分钟过一遍核心代码（trait 设计、async 数据流） |
| 10 天太紧 | 主动学习降级为统计报告；embedding 检索砍掉换 FTS5+jieba；知识图谱/概念关联已砍（跨课程检索用 all 区并集覆盖）；闪卡与大纲交互视图 V2 为第一顺位可砍项 |

---

## 附录 A：关键技术决策记录（唯一事实源）

> 正文各处只引用本附录编号；改决策只改这里。AGENTS.md 与正文冲突时以本附录为准。

### D1 中文检索 = jieba-rs 预分词 + FTS5 默认 tokenizer

- **决策**：入库与查询前都用 jieba 切词、空格连接后写入/查询 FTS5（`notes_fts` 的 content 列存预分词文本）；notes 表另存清洗后原文。
- **理由**：FTS5 默认 unicode61 把连续中文当一个 token，基本不可用；trigram 最小匹配 3 字符，"内存""所有权"这类高频 2 字词无法命中——中文高频词恰以 2 字为主。
- **否决备选**：trigram tokenizer、纯 LIKE 全表扫、外置向量库（重且非必需）。
- **实现细则**：
  - 入库与查询必须过**同一条**分词管线
  - 拼 MATCH 语法时每个词加双引号（`"内存" "所有权"`），否则含 `-`、`"` 的词导致语法错误
  - MATCH 多词默认 AND，长问题易零命中：先用 AND，结果不足 top-k 时降级 OR，按"命中词数 + bm25"排序
  - "命中词数" FTS5 不直接提供：OR 降级时对每词单独 COUNT 或自行统计，工作量勿低估

### D2 存储并发 = rusqlite 同步 API + spawn_blocking

- **决策**：storage crate 保持同步 API；所有调用点经 `tokio::task::spawn_blocking` 进 blocking 线程池；PDF 子进程等待同理。
- **理由**：rusqlite 直接在 tokio 主循环调用会阻塞 TUI 渲染（FTS5 大表查询尤其明显）；开 `bundled` feature 自带 FTS5。
- **否决备选**：sqlx（异步但更重，收益不匹配）；独立 DB 线程 + mpsc 请求通道（工作量大，仅 spawn_blocking 实测出瓶颈时升级）。

### D3 PDF 提取 = pymupdf 子进程

- **决策**：Rust 经 `tokio::process::Command` 调用自带 `scripts/pdf_extract.py`，stdout 输出 JSON（页数 + 逐页文本），天然支持按页进度上报。
- **理由**：中文课件提取质量经实测最好；pip wheel 跨平台。
- **否决备选**：纯 Rust PDF crate（中文提取质量差）、OCR（不做）。
- **注意**：子进程显式设 `PYTHONIOENCODING=utf-8`（Windows 默认 GBK 乱码）；Windows 命令可能是 `py -3` 而非 `python`；pymupdf 为 AGPL，文档注明；本机 pymupdf ≥1.28 后旧名 `import fitz` 已弃用，脚本统一用 `import pymupdf`。降级链见"导入流水线③"。

### D4 LLM 结构化输出 = 统一 JSON mode 降级链

- **决策**：所有 JSON 调用（归类/概念抽取、出题、批改、大纲）共用一条降级链：
  `response_format: json_object` → 不支持/报错则 prompt 约束"只输出 JSON"+ 正则提取第一个完整 `{...}` 块 → 解析失败重试一次 → 再失败跳过该条（只存原文 / 标记失败）。
- **理由**：并非所有 OpenAI-compatible endpoint 都支持 json_object；单点实现避免四处散落容错逻辑。

### D5 分类维度 = 课程唯一主分类

- **决策与理由**：见正文"分类体系设计决策"。要点：课程是复习/大纲的天然边界；LLM 归类到封闭列表远比开放标签稳定；不做辅助标签与树状层级。

### D6 工具边界 = 命令直连流水线，仅检索工具进 agent loop

- **决策**：`/import` `/review` `/delete` `/move` 直连内部函数；仅 `search_notes` / `list_courses` / `generate_outline` 暴露给 LLM。
- **理由**：不让 LLM 自主决定批量操作与费用敏感行为，可靠且可测试；详见正文"工具与命令的边界"。
