# 检索评测报告

- 题集: eval/questions.jsonl（31 题）｜ 检索范围: 全库 chunk 级 FTS（top5）
- 汇总: **rank1 命中 22/31** ｜ top3 命中 27/31 ｜ top5 命中 28/31 ｜ 平均耗时 4ms

| # | 问题 | 结果 | 命中 rank |
|---|---|---|---|
| 1 | 为什么函数调用之后栈指针 rsp 会变化 | ✗ | - | 0/10 |
| 2 | 补码是怎么表示负数的 | ✓ | 1 | 9/10 |
| 3 | IEEE 754 浮点数精度问题 | ✓ | 1 | 9/10 |
| 4 | 大小端字节序是什么 | ✓ | 1 | 6/10 |
| 5 | 如何提高 cache 命中率 | ✓ | 1 | 10/10 |
| 6 | 写矩阵乘法怎么优化局部性 | ✓ | 1 | 9/10 |
| 7 | 循环展开为什么能加速 | ✓ | 1 | 0/10 |
| 8 | 页表的作用是什么 | ✗ | - | 0/10 |
| 9 | TLB 是干什么的 | ✓ | 1 | 9/10 |
| 10 | malloc 底层怎么实现 | ✓ | 1 | 9/10 |
| 11 | 内部碎片和外部碎片的区别 | ✓ | 1 | 9/10 |
| 12 | 内存泄漏怎么排查 | ✓ | 1 | 6/10 |
| 13 | 静态库和共享库有什么区别 | ✓ | 1 | 8/10 |
| 14 | 链接时符号重复定义会怎样 | △ | 2 | 6/10 |
| 15 | fork 返回两次是什么意思 | △ | 2 | 9/10 |
| 16 | 信号处理函数里能做什么 | △ | 3 | 0/10 |
| 17 | 进程异常退出的原因有哪些 | △ | 2 | 8/10 |
| 18 | 文件描述符是什么 open read write | ✓ | 1 | 9/10 |
| 19 | shell 怎么实现重定向和管道 | ✓ | 1 | 6/10 |
| 20 | socket 编程的完整流程 | ✓ | 1 | 2/10 |
| 21 | TCP 和 UDP 的区别 | ✓ | 1 | 2/10 |
| 22 | 线程和进程的区别 | △ | 3 | 0/10 |
| 23 | 信号量的 P V 操作 | ✓ | 1 | 9/10 |
| 24 | 死锁的四个必要条件 | △ | 5 | 0/10 |
| 25 | 什么是竞态条件 怎么避免 | ✗ | - | 9/10 |
| 26 | GDB 怎么打断点和查看内存 | ✓ | 1 | 2/10 |
| 27 | 缓冲区溢出攻击的原理 | ✓ | 1 | 9/10 |
| 28 | Rust 所有权转移之后原变量还能用吗 | ✓ | 1 | 9/10 |
| 29 | Rust 的借用规则 & 和 &mut 有什么限制 | ✓ | 1 | 9/10 |
| 30 | Rust 的 Option 枚举怎么处理空值 | ✓ | 1 | 9/10 |
| 31 | Rust 里 String 和 &str 有什么区别 | ✓ | 1 | 8/10 |

## Miss / 低 rank 题目分析

- **#1 为什么函数调用之后栈指针 rsp 会变化** — 实际 top:
  - [1] 第5章-程序优化(函数调用阻碍代码移动（Fun)
  - [2] 工具-GDB速查(4. 查看寄存器)
  - [3] 工具-GDB速查(3. 执行控制)
  - [4] 工具-GDB速查(7. 栈操作)
  - [5] 文件IO基础(12. 常见坑)
- **#8 页表的作用是什么** — 实际 top:
  - [1] 02-own-struct(2.3 模式匹配)
  - [2] 第7章-链接-2(4. 符号与符号解析)
  - [3] 03-stdlib(3.3 错误处理)
  - [4] 03-stdlib(3.1 字符串)
  - [5] 第9章-动态内存分配(14. 内存危害与调试工具)
- **#24 死锁的四个必要条件** — 实际 top:
  - [1] 第3章-程序的机器级表示(6. 条件码与条件传送)
  - [2] 第三讲课后练习解析(第 2 题：HashMap )
  - [3] 03-stdlib(3.1 字符串)
  - [4] 第三讲课后练习解析(第 1 题：惰性迭代器与短路)
  - [5] 第12章-并发编程()
- **#25 什么是竞态条件 怎么避免** — 实际 top:
  - [1] 02-own-struct(2.1 所有权)
  - [2] 第8章-异常控制流(显式等待信号)
  - [3] 第7章-链接(.h 文件里的 extern)
  - [4] 第3章-程序的机器级表示(14. 习题与思考)
  - [5] 第3章-程序的机器级表示(边界检查与间接跳转)

## LLM 评测汇总

- 平均 judge 评分: **6.1/10**（31/31 题）
- 幻觉判定: 0 题
- 检索 miss（top3 外）仍答对（≥6 分）: 1 题——说明部分问题模型可凭自身知识兜底，检索价值需按题分析
- 总成本: ¥0.4283

### 每题明细

#### #1 为什么函数调用之后栈指针 rsp 会变化（检索 miss，0.0 分，¥0.0113）

- judge: 未回答函数调用后栈指针变化的原因，属于答非所问。
- 回答摘录: 根据提供的资料，没有直接解释“为什么函数调用之后栈指针 rsp 会变化”。资料 [1] 仅提到函数调用有副作用风险、编译器不能把调用移出循环；资料 [2][3][4] 只介绍了用 GDB 查看寄存器和栈的操作方法，并未说明 rsp 变化的机制。因此，无法依据现有资料回答该问题。
- 引用: 第 5 章：程序优化（Code Optimization）(函数调用阻碍代码移动（Function Calls Block Code Motion）)、GDB 快速入门(4. 查看寄存器)、GDB 快速入门(3. 执行控制)、GDB 快速入门(7. 栈操作)

#### #2 补码是怎么表示负数的（检索 rank1，9.0 分，¥0.0120）

- judge: 直接且正确解释了补码表示负数的原理，并补充了相关性质，无编造内容。
- 回答摘录: 补码表示负数时，**最高位（符号位）的权重为负数**，因此最高位为 1 的数即为负数 [4]。对一个补码整数取负，等价于“按位取反再加一”：  
$$-x = \sim x + 1$$ [2]。  
例如，补码值域不对称，`-TMin = TMin`，因为 `TMin`（即 `100…0₂`）取反得 `TMax`，加一后溢出回到 `TMin` [2]。另外，负数右移时采用算术右移，左侧补符号位以保持符号 [3]。
- 引用: 第 2 章：信息的表示与处理(取负运算（Negation）)、第 2 章：信息的表示与处理(右移 `x >> y`)、第 2 章：信息的表示与处理(总结)

#### #3 IEEE 754 浮点数精度问题（检索 rank1，9.0 分，¥0.0152）

- judge: 回答准确且全面，覆盖表示结构、舍入误差、结合律失效等核心点，逻辑清晰，无编造事实。
- 回答摘录: 根据提供的资料，IEEE 754 浮点数精度问题可概括为以下几点：

1. **表示结构**：IEEE 754 浮点数采用 `(-1)^s × M × 2^E` 形式编码，其中尾数 `M` 在 `[1.0, 2.0)` 范围内，指数 `E` 由偏移量决定。单精度尾数字段 23 位，双精度尾数字段 52 位，有限的尾数位数决定了精度上限 [1]。

2. **舍入导致精度损失**：浮点数运算结果需要舍入到目标位数，默认使用“向最近偶数舍入”模式，例如十进制 `1.2349999 → 1.23`，`1.2350000 → 1.24`。这种舍入本身会引入误差 [2]。

3. **结合律不成立**：…
- 引用: 第 2 章：信息的表示与处理(基本格式)、第 2 章：信息的表示与处理(舍入（Rounding）)、第 2 章：信息的表示与处理(浮点数运算的陷阱)、CSAPP 暑假自学计划(Ch2 - 信息的表示和处理 ⭐⭐⭐)

#### #4 大小端字节序是什么（检索 rank1，6.0 分，¥0.0223）

- judge: 方向正确但回答不完整，未给出大端序的具体定义，仅说明资料不足，属于有遗漏。
- 回答摘录: 根据提供的资料：

- **小端序**：最低有效字节（LSB）存放在最低地址。x86 全系列和 ARM（默认模式）都是小端序。[1]
- **大端序**：资料中仅提到网络字节序为大端，IP 地址等跨机器传输的整数以网络字节序存储。[4] 但未给出大端序的具体字节排列定义；资料[1] 说明完整讲解在第 2 章，但该内容未包含在提供的资料中。

因此，小端序的定义可直接回答；大端序仅能得知其用于网络字节序，具体规则在提供资料中不足。
- 引用: 第 3 章：程序的机器级表示(字节序可视化)、第 11 章：网络编程（Network Programming）(IP 地址)

#### #5 如何提高 cache 命中率（检索 rank1，10.0 分，¥0.0202）

- judge: 直接且正确回答了提高 cache 命中率的方法，基于局部性原理并给出具体可操作原则，无编造内容。
- 回答摘录: 根据资料，提高 cache 命中率的核心思路是**利用局部性原理**，让缓存的上层（更快、更小的存储）更多地承接对下层（更慢、更大的存储）的访问 [1]。具体可操作的原则如下：

1. **聚焦“常见情况”**：重点关注核心函数的内层循环，因为这是访问最密集、对性能影响最大的部分（“让常见情况跑得快”）[2]。

2. **最小化内层循环的缺失率**：
   - **利用时间局部性**：在循环内**重复引用**同一个变量，让该变量在首次加载后被缓存保留，后续访问直接命中 [2]。
   - **利用空间局部性**：采用 **stride-1 引用模式**（即顺序、连续地访问内存地址，例如按数组…
- 引用: 第 6 章：存储器层级(存储层级中的缓存实例)、第 6 章：存储器层级(写缓存友好代码（Writing Cache Friendly Code）)、第 6 章：存储器层级(性能指标（Cache Performance Metrics）)

#### #6 写矩阵乘法怎么优化局部性（检索 rank1，9.0 分，¥0.0187）

- judge: 回答正确给出循环重排和分块两种经典优化方法，并附有代码示例和缓存分析，内容完整可靠。
- 回答摘录: 优化矩阵乘法的局部性主要有两种方法：

1. **重排循环顺序（使用 `kij`）**  
   让内层循环**沿行主序（stride-1）访问两个输入矩阵**，仅靠重排循环顺序就能提升空间局部性，使缓存缺失率从 1.25 次/迭代 降到 0.5 次/迭代 [1]。

2. **分块（Tiling/Blocked）**  
   把一次缓存不下的整行计算切成 $B \times B$ 的小块，使**两个输入块 + 一个输出块**同时驻留缓存，条件为 $3B^2 < C$（$C$ 为缓存容量）[1]。  
   分块后缓存缺失从无分块时的 $(9/8)n^3$ 降到 $n^3/(4B)$ [1]…
- 引用: 第 6 章：存储器层级(分块总结)、第 6 章：存储器层级(分块矩阵乘法（Blocked Matrix Multiplication）)

#### #7 循环展开为什么能加速（检索 rank1，0.0 分，¥0.0122）

- judge: AI未回答问题，仅说明资料缺失，属于答非所问。
- 回答摘录: 资料中没有直接讲解“循环展开”的内容。资料 [4] 只提到内联（Inlining）等优化手段及其原理，并总结“五大基本优化”为常量折叠、死代码消除、CSE、代码移动、内联，但未涉及循环展开的机制或加速原因。因此，依据现有资料无法回答“循环展开为什么能加速”，需要补充相关资料。
- 引用: 第 5 章：程序优化（Code Optimization）(内联（Inlining）)

#### #8 页表的作用是什么（检索 miss，0.0 分，¥0.0080）

- judge: 未依据资料提供页表作用的任何信息，未回答问题
- 回答摘录: 资料中未包含关于页表（page table）的内容，因此无法依据所给资料回答页表的作用。

#### #9 TLB 是干什么的（检索 rank1，9.0 分，¥0.0104）

- judge: 回答直接且正确解释了TLB的作用和工作原理，符合高分标准。
- 回答摘录: TLB（Translation Lookaside Buffer，翻译后备缓冲）是 MMU 内部的一个小组相联硬件缓存，专门用来加速虚拟地址到物理地址的翻译：它把虚拟页号（VPN）直接映射到物理页号（PPN），保存少量页的完整页表项。MMU 每次翻译地址时先查 TLB，如果 TLB 命中，就无需再访问页表，省去一次缓存/内存访问；TLB 未命中时才需要额外访问页表取页表项（PTE），但由于程序访问具有时间局部性，TLB 未命中通常很罕见 [5]。此外，TLB 是虚拟内存实现中硬件/软件协同的一部分，硬件包括 MMU、TLB、异常机制，而 TLB 管理由软件（内核）完成 [1]。
- 引用: 第 9 章：虚拟内存（Virtual Memory）(总结)、第 9 章：虚拟内存（Virtual Memory）(7. TLB：加速翻译)

#### #10 malloc 底层怎么实现（检索 rank1，9.0 分，¥0.0180）

- judge: 回答正确且全面，涵盖了malloc向操作系统申请内存和分配器管理细节，无编造。
- 回答摘录: 根据资料，`malloc` 的底层实现主要涉及两个层面：**向操作系统要内存**和**用分配器管理这些内存**。

- 向操作系统要内存：`malloc` 内部靠 `sbrk` / `mmap` 向操作系统申请内存 [1]。
- 用空闲链表管理块、合并碎片：这是动态内存分配器的核心工作 [1]。

具体来说，一个分配器（即实现 `malloc` 的底层机制）必须解决五个问题 [4]：

1. **给一个指针，怎么知道要释放多少内存？** —— 标准做法是在块前面存一个 **header 字段**，记录块的长度（字节数，含 header）[4]。
2. **怎么跟踪空闲块？** —— 有四种方法…
- 引用: malloc / calloc / free（动态内存分配）(6. 和 CSAPP 第 9 章的关系)、第 9 章（补充）：动态内存分配（Dynamic Memory Allocation）(5. 实现问题概览)

#### #11 内部碎片和外部碎片的区别（检索 rank1，9.0 分，¥0.0132）

- judge: 回答准确清晰，结合资料解释两种碎片的定义、来源、可测量性并举例，无明显错误。
- 回答摘录: 根据资料 [1]，内部碎片与外部碎片的区别如下：

- **内部碎片 (internal fragmentation)**：某块的 **payload 小于块大小**，即块内空间被浪费。其来源包括：维护堆数据结构的开销、对齐填充（padding）、显式策略决定（如用大块满足小请求）。它只依赖**过去的请求模式**，因此**容易测量**。例如讲义基准中“完美分配器”因内部碎片也有 1.5% 开销 [1]。
- **外部碎片 (external fragmentation)**：**总空闲内存足够，但没有单个空闲块够大**。它依赖**未来的请求模式**，所以**难以测量**。例如：`p1 = ma…
- 引用: 第 9 章（补充）：动态内存分配（Dynamic Memory Allocation）(4. 碎片化)

#### #12 内存泄漏怎么排查（检索 rank1，6.0 分，¥0.0076）

- judge: 回答指出了资料不足，但没有给出实际排查方法，因此直接性不足。
- 回答摘录: 根据提供的资料，仅提到内存泄漏的原因是“忘了 `free`”，并且后果是“长期运行越吃越多” [4]。但资料中**没有提供任何具体的内存泄漏排查方法**（如使用工具、分析代码等），因此无法依据现有资料回答“怎么排查”。建议参考其他资料或工具说明。
- 引用: malloc / calloc / free（动态内存分配）(3. free：配对的另一半)

#### #13 静态库和共享库有什么区别（检索 rank1，8.0 分，¥0.0118）

- judge: 直接且正确回答了静态库与共享库的核心区别，但仅基于资料未展开更多细节。
- 回答摘录: 根据资料，静态库与共享库的主要区别在于链接方式和运行时内存使用：

- **静态链接**：可执行文件与运行内存映像**只包含实际用到的库代码**。也就是说，静态库的代码在链接时被复制进可执行文件中。[1]
- **动态链接**：可执行文件**不包含库代码**；运行时**单份库代码**被所有执行进程**共享**。[1]

因此，静态库会将所用代码嵌入可执行文件，而共享库则在运行时由多个进程共享同一份库代码。资料中未进一步说明其他差异（如文件后缀、更新方式等），如需更多细节需补充资料。
- 引用: 第 7 章：链接（Linking）(为什么需要链接器？)

#### #14 链接时符号重复定义会怎样（检索 rank2，6.0 分，¥0.0120）

- judge: 回答正确指出了多个强符号不允许，但未说明具体错误表现，信息不完整。
- 回答摘录: 根据资料，链接时若出现多个同名**强符号**（例如已初始化的全局变量），会违反规则“多个强符号不允许”，从而产生链接问题 [4]。资料还提到，定义全局变量时初始化会使其成为强符号 [2][3]，因此多个源文件中若都这样定义同名全局变量，就可能触发该规则。

不过，资料只说明了“多个强符号不允许”，并未详述具体会报什么错、链接器如何终止等行为，因此这部分信息不足，无法进一步说明。
- 引用: 第 7 章：链接（Linking）(最佳实践)、第 7 章：链接（Linking）(全局变量的最佳实践)、第 7 章：链接（Linking）(局部符号与 static)

#### #15 fork 返回两次是什么意思（检索 rank2，9.0 分，¥0.0100）

- judge: 直接且正确解释了fork返回两次的含义，包含父进程与子进程的返回值，无编造。
- 回答摘录: “fork 返回两次”的意思是：`fork` 虽然只被调用一次，但它会创建一个子进程，之后**父进程和子进程都会从 `fork` 调用处返回**，因此从调用者的角度看像是返回了两次。具体来说，父进程返回子进程的 PID，子进程返回 0。[2]
- 引用: 第 8 章：异常控制流（Exceptional Control Flow）(创建进程：fork)

#### #16 信号处理函数里能做什么（检索 rank3，0.0 分，¥0.0103）

- judge: 未回答问题，仅表示资料不足，没有提供有效内容。
- 回答摘录: 根据提供的资料，没有具体说明信号处理函数里能做什么。[5] 仅提到 `setjmp`/`longjmp` 常用于错误恢复与信号处理，但未展开信号处理函数内部可执行的操作。资料不足，无法回答该问题。
- 引用: 第 8 章：异常控制流（Exceptional Control Flow）(9. 非局部跳转)

#### #17 进程异常退出的原因有哪些（检索 rank2，8.0 分，¥0.0208）

- judge: 基于资料准确总结了系统调用失败导致的异常退出，但未涵盖信号等其他常见原因，略有遗漏。
- 回答摘录: 根据提供的资料，进程异常退出的原因主要包括：

1. **系统调用失败后显式退出**：几乎所有系统级操作都可能失败（唯一例外是返回 `void` 的操作），出错时大多返回 `−1` 并设置全局变量 `errno` 表示原因。若程序在检测到失败后调用 `exit(1)` 退出，即属于异常退出。[2][5]

2. **系统调用失败未被正确处理**：资料强调必须显式检查失败，否则程序可能继续执行错误的逻辑，最终导致异常行为。虽然资料未直接说明这必然导致进程退出，但明确指出“几乎每个系统调用都可能失败”，若未检查错误，程序可能以异常方式终止。[2][5]

3. **错误处理包装中主动退出**：教材…
- 引用: 第 8 章：异常控制流（Exceptional Control Flow）(系统调用数量与错误处理)、第 8 章：异常控制流（Exceptional Control Flow）(系统调用错误处理)

#### #18 文件描述符是什么 open read write（检索 rank1，9.0 分，¥0.0188）

- judge: 正确解释文件描述符及open/read/write核心行为，并明确说明资料未覆盖细节，无编造。
- 回答摘录: 根据资料，可以这样理解：

- **文件描述符**：它是进程**描述符表**中的条目（一个整数编号），该条目指向内核**打开文件表**中的某个打开文件表项 [3]。可以理解为“个人通讯录”里的一个编号，通过这个编号能找到对应的打开文件 [3]。
- **open**：用于打开一个文件，并返回一个文件描述符（例如 `open` 后得到 fd 4）[2]。同一个文件被 `open` 两次，会产生**两个不同的打开文件表项**，各自拥有独立的文件位置 [1]。
- **read**：通过文件描述符从文件读取数据。读取时使用该描述符对应**打开文件表项中的文件位置**，并且不同打开文件表项的文件位置相…
- 引用: 第 10 章：系统级 I/O（System Level I/O）(10. 文件共享与 fork)、第 10 章：系统级 I/O（System Level I/O）(11. I/O 重定向：dup2)、第 10 章：系统级 I/O（System Level I/O）(9. 内核如何表示打开文件)

#### #19 shell 怎么实现重定向和管道（检索 rank1，6.0 分，¥0.0159）

- judge: 重定向回答正确，但管道部分因资料不足未完整回答，方向正确有遗漏。
- 回答摘录: 重定向可以用 `dup2` 实现：在子进程执行 `exec` 之前，先 `open` 目标文件（得到一个新文件描述符，如 fd 4），再调用 `dup2(4, 1)` 使 fd 1（标准输出）与 fd 4 指向同一文件，之后写入 stdout 的内容就进入重定向目标文件，例如 `ls > foo.txt` 就是这样实现的 [1]。

关于“管道”的实现，现有资料只提到管道属于文件类型之一，以及读写管道时可能发生 short count [2][3]，但未说明 shell 如何用 `pipe()`、`fork()`、`dup2()` 等系统调用具体实现管道。因此，这个问题在提供的资料中信息不足，…
- 引用: 第 10 章：系统级 I/O（System Level I/O）(11. I/O 重定向：dup2)、第 10 章：系统级 I/O（System Level I/O）(5. Short Counts)、第 10 章：系统级 I/O（System Level I/O）(3. 文件类型、目录与路径)

#### #20 socket 编程的完整流程（检索 rank1，2.0 分，¥0.0183）

- judge: 未直接回答socket编程完整流程，仅说明资料不足，缺乏有效内容。
- 回答摘录: 根据提供的资料，无法完整说明 socket 编程的完整流程。资料中仅包含与 socket 相关的零散信息：

- [1] 描述了 Tiny Web 服务器的操作流程：接受客户端连接 → 读请求（经已连接 socket）→ 解析请求 → 根据 URI 提供动态或静态内容。这涉及服务器端接受连接、读写已连接 socket 等步骤，但没有给出 socket 编程的全流程（如创建 socket、绑定、监听、连接、关闭等）。
- [4] 提到读写网络 socket 会发生 short count，并建议使用 RIO 包处理，但同样未提供完整流程。

因此，关于 socket 编程的完整流程（例如 soc…
- 引用: 第 11 章：网络编程（Network Programming）(10. Tiny Web 服务器)、第 10 章：系统级 I/O（System Level I/O）(5. Short Counts)

#### #21 TCP 和 UDP 的区别（检索 rank1，2.0 分，¥0.0108）

- judge: 未提供UDP信息，没有回答问题，但未编造事实。
- 回答摘录: 根据提供的资料，仅能确认 TCP（面向连接的协议）的部分特性：

- **点对点**：连接一对进程 [3]
- **全双工**：数据可同时双向流动 [3]
- **可靠**：源发送的字节流最终被目的以相同顺序收到 [3]

资料中**没有提供任何关于 UDP 的描述**，因此无法基于现有资料对 TCP 和 UDP 进行比较。若要回答两者的区别，需要补充关于 UDP 的可靠资料。
- 引用: 第 11 章：网络编程（Network Programming）(5. 连接：socket 与端口)

#### #22 线程和进程的区别（检索 rank3，0.0 分，¥0.0147）

- judge: 未回答问题，仅说明资料不足，没有提供进程与线程的区别
- 回答摘录: 根据现有资料，**无法回答“线程和进程的区别”**。

资料中仅在第 12 章并发编程的信号量部分提到了“线程”（如“若有线程在 P 内等待”“任何线程可 P、任何线程可 V”[3]），但**没有给出进程的定义、线程的定义，也没有对比两者的区别**。其余资料涉及 Rust 常量、整数运算、移位指令、比较指令等，均与本题无关。

如需回答该问题，请提供关于“进程与线程”的专门资料。
- 引用: 第 12 章：并发编程（Concurrent Programming）(13. 信号量)

#### #23 信号量的 P V 操作（检索 rank1，9.0 分，¥0.0149）

- judge: 回答直接正确，涵盖P/V操作定义、典型用法，仅细节略简但无明显错误。
- 回答摘录: 信号量的 P、V 操作定义如下 [3]：

- **P(s)**（对应 `sem_wait`，荷兰语“尝试减少”）：若信号量 `s` 为 0，则等待某个 V 操作发生；之后 `s` 减 1 并返回。
- **V(s)**（对应 `sem_post`，荷兰语“增加”）：`s` 加 1；若有线程在 P 内等待，唤醒其中一个。

信号量是互斥锁的推广，不要求 P 必须在 V 之前调用，任何线程都可执行 P 或 V，无顺序要求 [3]。

典型用法包括：
- **互斥**：将信号量初值设为 1，用 `sem_wait`/`sem_post` 包住临界区，与互斥锁用法相同 [3]。
- **事件同步**…
- 引用: 第 12 章：并发编程（Concurrent Programming）(生产者-消费者队列)、第 12 章：并发编程（Concurrent Programming）(事件信号量)、第 12 章：并发编程（Concurrent Programming）(13. 信号量)

#### #24 死锁的四个必要条件（检索 rank5，0.0 分，¥0.0088）

- judge: AI 未回答死锁四必要条件，仅表示无资料，属于答非所问。
- 回答摘录: 根据提供的资料，没有找到关于“死锁的四个必要条件”的任何内容，因此无法回答。建议查阅操作系统或并发编程相关章节。

#### #25 什么是竞态条件 怎么避免（检索 miss，9.0 分，¥0.0152）

- judge: 直接且正确回答了竞态条件定义及避免方法，内容贴合资料，无编造。
- 回答摘录: 根据资料，竞态条件（race condition）出现在“检查条件”和“等待事件”两个操作之间不是原子的情况。例如：`while (!pid) pause();` 中，主程序先检查 `pid` 是否非零，然后进入 `pause()` 等待信号；如果信号恰好在“检查 pid”之后、“进入 pause”之前到达，那么信号不会被 `pause()` 捕获，程序可能永远挂起，这就是竞态 [2]。

避免方法（结合资料）：

- 使用 `sigsuspend` 原子地完成“设置信号掩码 + 暂停等待 + 恢复掩码”，避免检查与等待之间的间隙 [2]。
- 对共享数据结构的操作要阻塞相关信号，防止信号处理…
- 引用: 第 8 章：异常控制流（Exceptional Control Flow）(显式等待信号)

#### #26 GDB 怎么打断点和查看内存（检索 rank1，2.0 分，¥0.0091）

- judge: 未回答打断点的方法，只部分回答查看内存，遗漏主要问题。
- 回答摘录: 根据提供的资料，**打断点的方法未在其中说明**，无法回答。

查看内存的方法如下：
- 使用 `x` 命令查看内存内容，例如 `x/16gx $rsp` 可查看栈顶 16 个值（每个 8 字节），`x/16gx $rbp` 从 `%rbp` 开始查看 [3]。
- 可用 `display` 命令在每次断点停下时自动打印表达式，例如 `display x` 或 `display /x i`（十六进制显示）[2]。
- 引用: GDB 快速入门(display（自动打印）)、GDB 快速入门(7. 栈操作)

#### #27 缓冲区溢出攻击的原理（检索 rank1，9.0 分，¥0.0200）

- judge: 回答准确解释了缓冲区溢出原理，包含具体示例、栈布局和返回地址覆盖细节，并提及栈对齐注意事项，内容完整正确。
- 回答摘录: 缓冲区溢出攻击的核心是：向固定大小的缓冲区写入超过其容量的数据，导致数据覆盖相邻内存区域，从而劫持程序控制流。

以资料中的 `echo()` 函数为例 [1]：

```c
void echo() {
    char buf[4];      /* 只有 4 字节！*/
    gets(buf);         /* 读入任意长字符串 */
    puts(buf);
}
```

`gets` 不检查输入长度，因此可以向 `buf` 写入任意字节。栈上布局（低地址到高地址）为 `buf[0~3]`、20 字节未使用空间、8 字节返回地址 [1]。不同输入长度会产生不同后果 [1]：…
- 引用: 第 3 章：程序的机器级表示(溢出过程)、第 3 章：程序的机器级表示(栈的 16 字节对齐)

#### #28 Rust 所有权转移之后原变量还能用吗（检索 rank1，9.0 分，¥0.0117）

- judge: 直接正确回答了问题，解释清晰且符合语义，未编造资料中不存在的具体事实。
- 回答摘录: 根据资料，所有权转移是指“将所有权从一个变量转移到另一个变量”[2]。当所有权转移后，原变量不再拥有该值，因此不能再继续使用原变量来访问该数据。不过，资料中并未直接以“原变量能不能用”的措辞说明这一点，以上是根据所有权转移的语义得出的结论。
- 引用: 02-own-struct(2.1 所有权)

#### #29 Rust 的借用规则 & 和 &mut 有什么限制（检索 rank1，9.0 分，¥0.0114）

- judge: 直接且正确回答了问题，内容全面，无编造事实。
- 回答摘录: 根据资料，Rust 的借用规则对 `&` 和 `&mut` 的限制如下：

- 不能在某个对象不存在后继续保留对它的引用 [1]。
- 一个对象可以同时存在多个不可变引用（`&T`） [1][3]。
- 或者仅有一个可变引用（`&mut T`） [1][3]。
- 不可变引用与可变引用不能同时存在 [1][3]。

举例来说，在迭代集合时同时修改集合会违反借用规则，因为迭代器持有不可变借用，而修改操作需要可变借用，两者不能同时存在 [5]。此外，Rust 1.86 起提供 `get_disjoint_mut`，可以在符合借用规则的前提下同时获得多个互不相同元素的可变引用，或通过范围同时获得多个…
- 引用: 02-own-struct(2.1 所有权)、03-stdlib(3.4 容器)、02-own-struct(2.4 小结)、02-own-struct(2.1 所有权)

#### #30 Rust 的 Option 枚举怎么处理空值（检索 rank1，9.0 分，¥0.0099）

- judge: 回答直接且正确解释了 Option 及常见处理方式，符合评分标准；仅未展开更多细节，故扣一分。
- 回答摘录: Rust 通过 `Option<T>` 枚举处理空值。它定义为：

```rust
enum Option<T> {
    None,
    Some(T),
}
```

其中 `None` 表示空值，`Some(T)` 表示有值 [2]。Rust 推荐在需要返回空值时使用 `Option<T>`，而不是返回 `NaN`、`-1`、`null` 等特殊值 [2]。

常见的处理方式：

- 如果空值时有合理的默认值，可用 `unwrap_or(default)` 提供默认值 [1]。
- 如果希望对 `Option` 进行变换（有值时应用函数，空值时保持为空），可用 `map` [3]。
- 引用: 03-stdlib(3.2 Option 枚举类型)、03-stdlib(3.2 Option 枚举类型)、03-stdlib(3.2 Option 枚举类型)

#### #31 Rust 里 String 和 &str 有什么区别（检索 rank1，8.0 分，¥0.0145）

- judge: 回答正确概述了String与&str的核心区别，虽有细节省略但未发现编造
- 回答摘录: 根据资料，Rust 中 `String` 和 `&str` 的区别主要体现在以下几个方面：

1. **基本定义**：`String` 是在堆上分配空间、可以增长的字符序列；`&str` 是 `String` 的切片类型，字符串字面值（如 `"foo"`）都是 `&str` 类型 [2]。

2. **内存与大小**：`String` 分配在堆上，可以动态增长，实际上类似 `Vec<u8>` 外层包装 [3]。而 `str` 本身是编译时大小未知的类型，无法独立存在，只能通过引用（`&str`）来使用 [2][5]。

3. **获取方式**：可以通过对 `String` 取引用来获得 `&s…
- 引用: 01-basic(1.3 基本语法)、03-stdlib(3.1 字符串)、03-stdlib(3.1 字符串)、03-stdlib(3.1 字符串)


---

## 人工分析（基线结论，重跑会被覆盖）

**核心关联：rank1 命中题平均 7.2 分，非 rank1 题平均 3.6 分——检索质量与回答质量强相关（2 倍），检索仍是第一杠杆。**

**低分题归因（0 幻觉，回答行为全部正确）：**
- 语料覆盖缺口（#7 循环展开机制、#8 页表细节、#20 socket 完整流程、#21 UDP 对比、#1 栈帧布局）：笔记里本来就没写，检索再准也无米下锅。LLM 诚实声明资料不足而非编造——约束生效。
- 检索词法盲区（#16 #22 #24）：内容存在但词面不匹配排到 rank3-5。

**结论：**
1. 系统行为可信（0 幻觉，缺失即声明）；
2. 当前最大短板是**语料覆盖**（补笔记 > 改算法）；
3. 检索侧残余收益在词法盲区题——若补语料后 top3 仍 <90%，再考虑 hybrid；
4. 每次检索改进都应同时跑离线评测（命中率）与本评测（回答分），双证据链。
